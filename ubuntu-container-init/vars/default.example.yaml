---
ansible_ssh_private_key_file: "/some/path/to/key" # private key path
ansible_port: 1234 # initial ssh port
set_ssh_port: 1234 # what to set port to
ssh_key_path: "/some/path/to/key.pub" # public key path to be deployed
